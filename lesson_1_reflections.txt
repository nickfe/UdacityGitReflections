mac: diff -u oldfile newfile
可以对比新文件相对于旧文件有哪些改动
当新程序出现问题时，可以方便地根据改动来排错

手动提交版本会使得每次提交都是有意义的，但忘记提交、出现意外无法提交会遭到损失
自动保存版本可以防止忘记保存，防止意外出现，但每次保存的意义不明确

git log: 显示历史记录 q键退出
git diff： 同上面的diff，文件名可以用commit的编号；好像没有-u
